# Changelog

The root has no changelog. Each package owns its own — see
`packages/*/CHANGELOG.md`. Releases are managed by
[changesets](https://github.com/changesets/changesets); see
[`.github/RELEASING.md`](.github/RELEASING.md).
